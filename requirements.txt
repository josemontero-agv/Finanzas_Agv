# Finanzas AGV - Dependencias del proyecto
requests==2.32.3
# Framework Web
Flask==3.0.0
Werkzeug==3.0.1
Flask-CORS==4.0.0

# Configuración
python-dotenv==1.0.0

# Cliente Odoo (XML-RPC built-in de Python — soporta API Keys para usuarios con 2FA)

# Utilidades
python-dateutil==2.8.2

# Exportación a Excel
openpyxl==3.1.2
polars==1.41.0
XlsxWriter==3.2.9

# Envío de correos
Flask-Mail==0.9.1

# Optimización de Performance & Caching
Flask-Caching==2.1.0
Flask-Compress==1.14
redis==5.0.1

# Asincronía y Colas
celery==5.3.6

# Base de Datos y Cloud (Supabase)
# 2.31.0 (antes 2.3.0): el proyecto Supabase usa el nuevo formato de API key
# (sb_secret_...) en vez de las claves legacy basadas en JWT. supabase-py 2.3.0
# validaba localmente que la key tuviera formato JWT (regex con puntos) y
# lanzaba SupabaseException("Invalid API key") antes de siquiera intentar la
# conexión. Ese chequeo se eliminó en versiones posteriores (commit 2f4a5ed,
# 2025-06-20) para soportar las nuevas claves opacas sb_publishable_/sb_secret_.
supabase==2.31.0
psycopg2-binary==2.9.9
SQLAlchemy==2.0.23

# Seguridad
Flask-JWT-Extended==4.7.1
Flask-Limiter==3.9.0

# Autenticación OAuth2 (login con Google)
Authlib==1.7.2

# Servidor WSGI para producción (opcional)
gunicorn==21.2.0

# Testing (opcional)
pytest==7.4.3
pytest-flask==1.3.0

# Documentación (MkDocs)
mkdocs==1.6.1
mkdocs-material==9.7.0
